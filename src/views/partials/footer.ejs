</main>
  <footer class="site-footer">
    <div class="wrap footer-inner">
      <div>
        <p class="footer-brand"><%= siteName %></p>
        <p>&copy; <%= new Date().getFullYear() %> · Lightweight anime CMS · retro CRT theme</p>
      </div>
      <p class="footer-meta">
        <a href="<%= paths.rss() %>">RSS</a>
        ·
        <a href="<%= paths.sitemap() %>">Sitemap</a>
        ·
        <a href="<%= paths.about() %>">About</a>
        ·
        <a href="/health">Health</a>
      </p>
    </div>
  </footer>
  <script src="/js/main.js" defer></script>
  <% if (typeof pageScripts !== 'undefined' && pageScripts && pageScripts.length) { %>
    <% pageScripts.forEach(function (src) { %>
  <script src="<%= src %>" defer></script>
    <% }) %>
  <% } %>
</body>
</html>

