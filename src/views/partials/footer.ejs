</main>
  <footer class="site-footer">
    <div class="wrap footer-inner">
      <div>
        <p class="footer-brand"><%= siteName %></p>
        <p>&copy; <%= new Date().getFullYear() %> · Ainme — Anime in Me · Stay neon</p>
      </div>
      <p class="footer-meta">
        <a href="<%= paths.blog() %>">Blog</a>
        ·
        <a href="<%= paths.archive() %>">Archive</a>
        ·
        <a href="<%= paths.about() %>">About</a>
        ·
        <a href="<%= paths.contact() %>">Contact</a>
        ·
        <a href="<%= paths.rss() %>">RSS</a>
      </p>
    </div>
  </footer>
  <script src="/js/main.js" defer></script>
  <% if (typeof pageScripts !== 'undefined' && pageScripts && pageScripts.length) { %>
    <% pageScripts.forEach(function (src) { %>
  <script src="<%= src %>" defer></script>
    <% }) %>
  <% } %>
</body>
</html>
