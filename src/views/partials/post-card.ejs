<article class="post-card scanlines">
  <header class="post-card__header">
    <% if (post.categories && post.categories.length) { %>
    <p class="post-card__cats">
      <% post.categories.forEach(function (cat) { %>
      <a href="<%= paths.category(cat.slug) %>"><%= cat.name %></a>
      <% }) %>
    </p>
    <% } %>
    <h2 class="post-card__title">
      <a href="<%= post.url %>"><%= post.title %></a>
    </h2>
    <p class="post-card__meta">
      <% if (post.publishedLabel) { %><time datetime="<%= post.publishedAt || post.createdAt %>"><%= post.publishedLabel %></time><% } %>
      <% if (post.author) { %> · <%= post.author.displayName %><% } %>
    </p>
  </header>
  <% if (post.excerpt) { %>
  <p class="post-card__excerpt"><%= post.excerpt %></p>
  <% } %>
  <% if (post.tags && post.tags.length) { %>
  <p class="post-card__tags">
    <% post.tags.forEach(function (tag) { %>
    <a href="<%= paths.tag(tag.slug) %>">#<%= tag.slug %></a>
    <% }) %>
  </p>
  <% } %>
  <div class="post-card__footer">
    <a class="btn btn-ghost btn-sm post-card__read" href="<%= post.url %>">Read →</a>
    <% if (post.workTitle && post.workUrl) { %>
    <a class="post-card__work" href="<%= post.workUrl %>" title="All posts about <%= post.workTitle %>"><%= post.workTitle %></a>
    <% } else if (post.workTitle) { %>
    <span class="post-card__work" title="Anime / manga / work"><%= post.workTitle %></span>
    <% } %>
  </div>
</article>
