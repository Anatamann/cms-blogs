<%- include('../partials/header') %>

<section class="wrap page-block">
  <h1><%= status %> — Static on the line</h1>
  <p class="lead"><%= message %></p>
  <p>
    <a class="btn btn-primary" href="<%= paths.home() %>">Back to home</a>
    <a class="btn btn-ghost" href="<%= paths.blog() %>">Browse the blog</a>
  </p>
</section>

<%- include('../partials/footer') %>
