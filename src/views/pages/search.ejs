<%- include('../partials/header') %>

<div class="wrap layout-with-sidebar">
  <div class="layout-main">
    <header class="page-header">
      <h1>Search</h1>
      <form class="search-form" action="<%= paths.search() %>" method="get" role="search">
        <label class="sr-only" for="q">Search posts</label>
        <input id="q" type="search" name="q" value="<%= q %>" placeholder="Titles, excerpts, body…" autofocus />
        <button type="submit" class="btn btn-primary">Search</button>
      </form>
    </header>

    <% if (!q) { %>
    <p class="lead">Type a query to search published posts.</p>
    <% } else if (!posts.length) { %>
    <p class="lead">No results for <strong><%= q %></strong>.</p>
    <% } else { %>
    <p class="result-count"><%= pagination.total %> result<%= pagination.total === 1 ? '' : 's' %> for “<%= q %>”</p>
    <div class="post-grid">
      <% posts.forEach(function (post) { %>
        <%- include('../partials/post-card', { post: post }) %>
      <% }) %>
    </div>
    <%- include('../partials/pagination') %>
    <% } %>
  </div>
  <%- include('../partials/sidebar') %>
</div>

<%- include('../partials/footer') %>
