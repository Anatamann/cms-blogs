<%- include('../partials/header') %>

<section class="wrap page-block">
  <h1><%= heading %></h1>
  <p class="lead"><%= message %></p>
  <p><a class="btn btn-ghost" href="<%= paths.home() %>">← Back home</a></p>
</section>

<%- include('../partials/footer') %>
